import 'package:flutter/material.dart';

class PdfUploadScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Upload File'),
      ),
      body: Center(
        child: Text('File Upload UI Goes Here'),
      ),
    );
  }
}
